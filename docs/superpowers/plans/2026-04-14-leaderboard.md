# Leaderboard React Native App — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a single-screen React Native (Expo) leaderboard app showing a top-3 podium and a full ranked list of all participants, with mock data and a clean swap point for a real API.

**Architecture:** Expo managed workflow with TypeScript and Expo Router. Data fetching is isolated in `data/leaderboard.ts` behind a `fetchLeaderboard()` function — all components consume that interface, so the real API is wired in by replacing one function. The screen uses an outer `ScrollView` with a non-scrolling inner `FlatList` to handle the combined podium + list layout.

**Tech Stack:** Expo SDK (latest), React Native, TypeScript, Expo Router, `@expo/vector-icons` (for trend icons)

---

## File Map

| File | Action | Responsibility |
|------|--------|---------------|
| `types/index.ts` | Create | `Participant`, `Badge`, `Trend` types |
| `constants/theme.ts` | Create | Color palette constants |
| `data/leaderboard.ts` | Create | Mock data + `fetchLeaderboard()` |
| `components/LeaderboardRow.tsx` | Create | Single ranked row (rank · avatar · name + badge · trend · score) |
| `components/LeaderboardList.tsx` | Create | FlatList of all participants with header label |
| `components/Podium.tsx` | Create | Top-3 podium visual |
| `app/index.tsx` | Create/Modify | Screen: fetch, state, compose Podium + LeaderboardList |

---

## Task 1: Bootstrap Expo project

**Files:**
- Modify: `package.json` (generated by Expo)
- Modify: `app.json` (generated by Expo)

- [ ] **Step 1: Create new Expo project**

Run inside `/home/vinayak/kact/leaderboard/`:
```bash
npx create-expo-app@latest . --template blank-typescript
```
When prompted to overwrite existing files, confirm yes (only `.git` and `docs/` exist).

- [ ] **Step 2: Install Expo Router**

```bash
npx expo install expo-router react-native-safe-area-context react-native-screens expo-linking expo-constants expo-status-bar
```

- [ ] **Step 3: Configure Expo Router in app.json**

Open `app.json` and ensure the `expo.scheme` field and `main` entry are set:
```json
{
  "expo": {
    "scheme": "leaderboard",
    "main": "expo-router/entry"
  }
}
```

- [ ] **Step 4: Update package.json main entry**

In `package.json`, set:
```json
{
  "main": "expo-router/entry"
}
```

- [ ] **Step 5: Verify project starts**

```bash
npx expo start --go
```
Expected: Metro bundler starts, QR code displayed. No errors in terminal.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: bootstrap Expo project with Expo Router"
```

---

## Task 2: Types and theme constants

**Files:**
- Create: `types/index.ts`
- Create: `constants/theme.ts`

- [ ] **Step 1: Create types/index.ts**

```typescript
// types/index.ts

export type Badge =
  | 'Quiz Master'
  | 'Code Ninja'
  | 'Scholar'
  | 'Streak'
  | 'Hacker'
  | 'Rising Star';

export type Trend = 'up' | 'down' | 'same';

export interface Participant {
  id: string;
  name: string;
  avatarUrl: string; // emoji string used in mock data; swap for real URL with API
  score: number;
  badge: Badge;
  trend: Trend;
  trendDelta: number; // spots moved, e.g. 2 means moved up 2 ranks
}
```

- [ ] **Step 2: Create constants/theme.ts**

```typescript
// constants/theme.ts

export const colors = {
  background: '#1a1a2e',
  card: '#16213e',
  row: '#2a2a4a',
  accent: '#6c63ff',
  gold: '#FFD700',
  silver: '#C0C0C0',
  bronze: '#CD7F32',
  trendUp: '#4CAF50',
  trendDown: '#F44336',
  trendSame: '#888888',
  textPrimary: '#FFFFFF',
  textSecondary: '#AAAAAA',
};
```

- [ ] **Step 3: Commit**

```bash
git add types/index.ts constants/theme.ts
git commit -m "feat: add Participant types and theme color constants"
```

---

## Task 3: Mock data and fetchLeaderboard()

**Files:**
- Create: `data/leaderboard.ts`

- [ ] **Step 1: Create data/leaderboard.ts**

```typescript
// data/leaderboard.ts

import { Participant } from '../types';

const MOCK_PARTICIPANTS: Participant[] = [
  { id: '1', name: 'Arjun',  avatarUrl: '😎', score: 1240, badge: 'Quiz Master', trend: 'same', trendDelta: 0 },
  { id: '2', name: 'Priya',  avatarUrl: '😊', score: 1180, badge: 'Code Ninja',  trend: 'up',   trendDelta: 2 },
  { id: '3', name: 'Rohan',  avatarUrl: '🙂', score: 1050, badge: 'Scholar',     trend: 'down', trendDelta: 1 },
  { id: '4', name: 'Sneha',  avatarUrl: '🤩', score: 940,  badge: 'Streak',      trend: 'up',   trendDelta: 3 },
  { id: '5', name: 'Kiran',  avatarUrl: '😏', score: 880,  badge: 'Hacker',      trend: 'down', trendDelta: 2 },
  { id: '6', name: 'Meera',  avatarUrl: '😄', score: 820,  badge: 'Rising Star', trend: 'up',   trendDelta: 1 },
  { id: '7', name: 'Dev',    avatarUrl: '🧐', score: 760,  badge: 'Scholar',     trend: 'same', trendDelta: 0 },
  { id: '8', name: 'Ananya', avatarUrl: '😃', score: 700,  badge: 'Quiz Master', trend: 'down', trendDelta: 1 },
  { id: '9', name: 'Raj',    avatarUrl: '🤓', score: 650,  badge: 'Code Ninja',  trend: 'up',   trendDelta: 2 },
  { id: '10', name: 'Lena',  avatarUrl: '😁', score: 580,  badge: 'Hacker',      trend: 'same', trendDelta: 0 },
];

/**
 * Fetch leaderboard participants sorted by score descending.
 * Replace this function body to wire up the real API — no component changes needed.
 */
export async function fetchLeaderboard(): Promise<Participant[]> {
  // Simulate network latency
  await new Promise((resolve) => setTimeout(resolve, 800));
  return [...MOCK_PARTICIPANTS].sort((a, b) => b.score - a.score);
}
```

- [ ] **Step 2: Commit**

```bash
git add data/leaderboard.ts
git commit -m "feat: add mock leaderboard data and fetchLeaderboard function"
```

---

## Task 4: LeaderboardRow component

**Files:**
- Create: `components/LeaderboardRow.tsx`

- [ ] **Step 1: Create components/LeaderboardRow.tsx**

```tsx
// components/LeaderboardRow.tsx

import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import { Participant } from '../types';
import { colors } from '../constants/theme';

interface Props {
  participant: Participant;
  rank: number;
}

function rankColor(rank: number): string {
  if (rank === 1) return colors.gold;
  if (rank === 2) return colors.silver;
  if (rank === 3) return colors.bronze;
  return colors.textSecondary;
}

function trendLabel(trend: Participant['trend'], delta: number): string {
  if (trend === 'up') return `↑${delta}`;
  if (trend === 'down') return `↓${delta}`;
  return '—';
}

function trendColor(trend: Participant['trend']): string {
  if (trend === 'up') return colors.trendUp;
  if (trend === 'down') return colors.trendDown;
  return colors.trendSame;
}

export default function LeaderboardRow({ participant, rank }: Props) {
  const isFirst = rank === 1;

  return (
    <View
      style={[
        styles.row,
        isFirst && styles.rowFirst,
      ]}
    >
      {/* Rank */}
      <Text style={[styles.rank, { color: rankColor(rank) }]}>{rank}</Text>

      {/* Avatar */}
      <View style={styles.avatar}>
        <Text style={styles.avatarEmoji}>{participant.avatarUrl}</Text>
      </View>

      {/* Name + Badge */}
      <View style={styles.nameContainer}>
        <Text style={styles.name}>{participant.name}</Text>
        <View style={styles.badgePill}>
          <Text style={styles.badgeText}>{participant.badge}</Text>
        </View>
      </View>

      {/* Trend */}
      <Text style={[styles.trend, { color: trendColor(participant.trend) }]}>
        {trendLabel(participant.trend, participant.trendDelta)}
      </Text>

      {/* Score */}
      <Text style={[styles.score, { color: isFirst ? colors.gold : colors.accent }]}>
        {participant.score}
      </Text>
    </View>
  );
}

const styles = StyleSheet.create({
  row: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: colors.row,
    borderRadius: 8,
    padding: 10,
    marginBottom: 6,
    gap: 8,
  },
  rowFirst: {
    backgroundColor: '#FFD70015',
    borderLeftWidth: 3,
    borderLeftColor: colors.gold,
  },
  rank: {
    width: 20,
    fontSize: 13,
    fontWeight: '800',
    textAlign: 'center',
  },
  avatar: {
    width: 36,
    height: 36,
    borderRadius: 18,
    backgroundColor: colors.card,
    alignItems: 'center',
    justifyContent: 'center',
  },
  avatarEmoji: {
    fontSize: 18,
  },
  nameContainer: {
    flex: 1,
  },
  name: {
    color: colors.textPrimary,
    fontSize: 13,
    fontWeight: '600',
  },
  badgePill: {
    backgroundColor: colors.card,
    borderRadius: 10,
    paddingHorizontal: 6,
    paddingVertical: 1,
    alignSelf: 'flex-start',
    marginTop: 2,
  },
  badgeText: {
    color: colors.textSecondary,
    fontSize: 9,
  },
  trend: {
    fontSize: 11,
    fontWeight: '600',
    width: 28,
    textAlign: 'right',
  },
  score: {
    fontSize: 13,
    fontWeight: '800',
    width: 36,
    textAlign: 'right',
  },
});
```

- [ ] **Step 2: Commit**

```bash
git add components/LeaderboardRow.tsx
git commit -m "feat: add LeaderboardRow component"
```

---

## Task 5: LeaderboardList component

**Files:**
- Create: `components/LeaderboardList.tsx`

- [ ] **Step 1: Create components/LeaderboardList.tsx**

```tsx
// components/LeaderboardList.tsx

import React from 'react';
import { View, FlatList, Text, StyleSheet } from 'react-native';
import { Participant } from '../types';
import LeaderboardRow from './LeaderboardRow';
import { colors } from '../constants/theme';

interface Props {
  participants: Participant[];
}

export default function LeaderboardList({ participants }: Props) {
  return (
    <View style={styles.container}>
      <Text style={styles.header}>ALL PARTICIPANTS</Text>
      <FlatList
        data={participants}
        keyExtractor={(item) => item.id}
        scrollEnabled={false}
        renderItem={({ item, index }) => (
          <LeaderboardRow participant={item} rank={index + 1} />
        )}
      />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    backgroundColor: colors.card,
    borderRadius: 10,
    padding: 10,
  },
  header: {
    color: colors.textSecondary,
    fontSize: 10,
    fontWeight: '700',
    letterSpacing: 1.5,
    marginBottom: 8,
    paddingHorizontal: 4,
  },
});
```

- [ ] **Step 2: Commit**

```bash
git add components/LeaderboardList.tsx
git commit -m "feat: add LeaderboardList component"
```

---

## Task 6: Podium component

**Files:**
- Create: `components/Podium.tsx`

- [ ] **Step 1: Create components/Podium.tsx**

```tsx
// components/Podium.tsx

import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import { Participant } from '../types';
import { colors } from '../constants/theme';

interface Props {
  top3: Participant[];
}

interface PodiumSlotProps {
  participant: Participant;
  position: 1 | 2 | 3;
}

const POSITION_CONFIG = {
  1: { medal: '🥇', podiumHeight: 60, avatarSize: 54, nameColor: colors.gold, podiumColor: colors.gold, showCrown: true },
  2: { medal: '🥈', podiumHeight: 44, avatarSize: 46, nameColor: colors.silver, podiumColor: '#4a4e69', showCrown: false },
  3: { medal: '🥉', podiumHeight: 32, avatarSize: 40, nameColor: colors.bronze, podiumColor: '#4a4e69', showCrown: false },
} as const;

function PodiumSlot({ participant, position }: PodiumSlotProps) {
  const config = POSITION_CONFIG[position];
  return (
    <View style={styles.slot}>
      {config.showCrown && <Text style={styles.crown}>👑</Text>}
      <View style={[styles.avatar, { width: config.avatarSize, height: config.avatarSize, borderRadius: config.avatarSize / 2 }]}>
        <Text style={styles.avatarEmoji}>{participant.avatarUrl}</Text>
      </View>
      <Text style={[styles.name, { color: config.nameColor }]} numberOfLines={1}>{participant.name}</Text>
      <Text style={styles.score}>{participant.score}</Text>
      <View style={[styles.podiumBlock, { height: config.podiumHeight, backgroundColor: config.podiumColor }]}>
        <Text style={[styles.medal, { color: config.nameColor }]}>{config.medal}</Text>
      </View>
    </View>
  );
}

export default function Podium({ top3 }: Props) {
  if (top3.length < 3) return null;
  const [first, second, third] = top3;

  return (
    <View style={styles.podium}>
      <PodiumSlot participant={second} position={2} />
      <PodiumSlot participant={first} position={1} />
      <PodiumSlot participant={third} position={3} />
    </View>
  );
}

const styles = StyleSheet.create({
  podium: {
    flexDirection: 'row',
    alignItems: 'flex-end',
    justifyContent: 'center',
    gap: 10,
    marginBottom: 20,
  },
  slot: {
    alignItems: 'center',
    width: 90,
  },
  crown: {
    fontSize: 16,
    marginBottom: 2,
  },
  avatar: {
    backgroundColor: colors.card,
    alignItems: 'center',
    justifyContent: 'center',
  },
  avatarEmoji: {
    fontSize: 22,
  },
  name: {
    fontSize: 11,
    fontWeight: '700',
    marginTop: 4,
    textAlign: 'center',
  },
  score: {
    fontSize: 10,
    color: colors.textSecondary,
    marginBottom: 4,
  },
  podiumBlock: {
    width: 70,
    borderRadius: 6,
    alignItems: 'center',
    justifyContent: 'center',
  },
  medal: {
    fontSize: 18,
    fontWeight: '900',
  },
});
```

- [ ] **Step 2: Commit**

```bash
git add components/Podium.tsx
git commit -m "feat: add Podium component for top-3 display"
```

---

## Task 7: Leaderboard screen (app/index.tsx)

**Files:**
- Create/Modify: `app/index.tsx`

- [ ] **Step 1: Create app/index.tsx**

```tsx
// app/index.tsx

import React, { useEffect, useState, useCallback } from 'react';
import {
  View,
  Text,
  ScrollView,
  ActivityIndicator,
  TouchableOpacity,
  StyleSheet,
  SafeAreaView,
  StatusBar,
} from 'react-native';
import { fetchLeaderboard } from '../data/leaderboard';
import { Participant } from '../types';
import { colors } from '../constants/theme';
import Podium from '../components/Podium';
import LeaderboardList from '../components/LeaderboardList';

export default function LeaderboardScreen() {
  const [participants, setParticipants] = useState<Participant[]>([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  const load = useCallback(async () => {
    setLoading(true);
    setError(null);
    try {
      const data = await fetchLeaderboard();
      setParticipants(data);
    } catch (e) {
      setError('Failed to load leaderboard. Please try again.');
    } finally {
      setLoading(false);
    }
  }, []);

  useEffect(() => {
    load();
  }, [load]);

  return (
    <SafeAreaView style={styles.safe}>
      <StatusBar barStyle="light-content" backgroundColor={colors.background} />
      <View style={styles.container}>
        {/* Header */}
        <Text style={styles.title}>🏆 LEADERBOARD</Text>

        {/* Loading */}
        {loading && (
          <View style={styles.centered}>
            <ActivityIndicator size="large" color={colors.accent} />
          </View>
        )}

        {/* Error */}
        {!loading && error && (
          <View style={styles.centered}>
            <Text style={styles.errorText}>{error}</Text>
            <TouchableOpacity style={styles.retryButton} onPress={load}>
              <Text style={styles.retryText}>Retry</Text>
            </TouchableOpacity>
          </View>
        )}

        {/* Empty */}
        {!loading && !error && participants.length === 0 && (
          <View style={styles.centered}>
            <Text style={styles.emptyText}>No participants yet.</Text>
          </View>
        )}

        {/* Content */}
        {!loading && !error && participants.length > 0 && (
          <ScrollView
            contentContainerStyle={styles.scroll}
            showsVerticalScrollIndicator={false}
          >
            <Podium top3={participants.slice(0, 3)} />
            <LeaderboardList participants={participants} />
          </ScrollView>
        )}
      </View>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  safe: {
    flex: 1,
    backgroundColor: colors.background,
  },
  container: {
    flex: 1,
    backgroundColor: colors.background,
    paddingHorizontal: 16,
  },
  title: {
    color: colors.textPrimary,
    fontSize: 16,
    fontWeight: '800',
    letterSpacing: 2,
    textAlign: 'center',
    paddingVertical: 16,
  },
  scroll: {
    paddingBottom: 32,
  },
  centered: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    gap: 16,
  },
  errorText: {
    color: colors.trendDown,
    fontSize: 14,
    textAlign: 'center',
  },
  retryButton: {
    backgroundColor: colors.accent,
    paddingHorizontal: 24,
    paddingVertical: 10,
    borderRadius: 8,
  },
  retryText: {
    color: colors.textPrimary,
    fontWeight: '700',
    fontSize: 14,
  },
  emptyText: {
    color: colors.textSecondary,
    fontSize: 14,
  },
});
```

- [ ] **Step 2: Commit**

```bash
git add app/index.tsx
git commit -m "feat: add Leaderboard screen with loading, error, and empty states"
```

---

## Task 8: Verify the app end-to-end

- [ ] **Step 1: Start the development server**

```bash
npx expo start --go
```

- [ ] **Step 2: Open on device or emulator**

Scan the QR code with Expo Go (iOS/Android) or press `i` for iOS simulator / `a` for Android emulator.

Expected:
- Dark background screen
- "🏆 LEADERBOARD" title
- Loading spinner appears for ~800ms
- Podium with Arjun (1st/center), Priya (2nd/left), Rohan (3rd/right)
- Full scrollable list of all 10 participants below
- Each row shows rank, emoji avatar, name, badge pill, trend arrow with delta, score

- [ ] **Step 3: Verify error state**

In `data/leaderboard.ts`, temporarily change `fetchLeaderboard` to throw:
```typescript
export async function fetchLeaderboard(): Promise<Participant[]> {
  await new Promise((resolve) => setTimeout(resolve, 800));
  throw new Error('Simulated error');
}
```
Expected: error message + Retry button appear. Tapping Retry re-triggers fetch.

- [ ] **Step 4: Restore fetchLeaderboard**

Revert the throw — restore the mock return from Task 3.

- [ ] **Step 5: Final commit**

```bash
git add -A
git commit -m "chore: verify leaderboard app end-to-end"
```
